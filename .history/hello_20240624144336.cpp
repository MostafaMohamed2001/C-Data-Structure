#include <iostream>
#include <cmath>
#include <string>

using namespace std;

int main() {
  string s, t;
  cin >> s>>t;
  int s_size = s.length();
  bool done = false;
 
 for (int i = 0; i < str.length(); i++) {
    if (str[i] == oldChar[0]) {
        str.replace(i, 1, newChar);
        i--; // to avoid skipping characters
    }
}
  return 0;
}

