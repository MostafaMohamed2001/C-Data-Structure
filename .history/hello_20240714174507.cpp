#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> arr(n);

    // Input array elements
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int left = 0, right = n - 1 , dima_count = 0 , serja=0;
    bool sirja_trun = true;

    while (left <= right) {

        if(arr[left] < arr)



    }
}
