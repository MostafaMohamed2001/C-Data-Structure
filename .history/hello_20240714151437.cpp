#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <math.h>
using namespace std;
int main() {
    int n , G =0;
    cin >> n;
    string f ,s;
    for (int i = 0; i < n; i++) {
        cin >> f[i] >> s[i];
    }
    for (int i = 0; i < n-1; i++){
        // if((s[i] != f[i+1]) || (s[i] ==' ')){
        //     G++;
        // }
            cout<<s
    }
    // cout << G << endl;
}
