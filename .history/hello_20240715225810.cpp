#include <iostream>
#include <string>
using namespace std;

int main() {
    int n , k , is;
    cin >> n >> k;
    for (int i = 0; i < n; ++i){
        string s;
        cin >> s;
        for (int i = 0; i < s.length(); i++){
            if((int)s[i] >= 0 && (int) s[i] <= k){

            }
        }
    }

        return 0;
}
