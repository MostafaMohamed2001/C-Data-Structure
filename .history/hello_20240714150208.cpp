#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <math.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    int f[n], s[n];
    for (int i = 0; i < n; i++) {
        cin >> f[i] >> s[i];
    }
    for (int i = 0; i < n; i++){
        co
    }
}
