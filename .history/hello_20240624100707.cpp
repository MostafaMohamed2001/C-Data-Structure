#include <iostream>
#include <string>
using namespace std;

int main() {
    int N, M;
    cin >> N >> M;
    
    string C[N];
    for (int i = 0; i < N; ++i) {
        cin >> C[i];
    }
    
    string D[M];
    for (int i = 0; i < M; ++i) {
        cin >> D[i];
    }
    
    int P0;
    cin >> P0;
    
    int P[M];
    for (int i = 0; i < M; ++i) {
        cin >> P[i];
    }
    int total = 0;
    for (int i = 0; i < N; i++)
    {
      bool found = false;
      for(int i=0)
    }
}
