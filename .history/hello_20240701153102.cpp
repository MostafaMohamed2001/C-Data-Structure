#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n, t, a;
    cin >> n >> t >> a;
    int h[n];
    for (int i = 0; i < n; i++){
        cin >> h[i];
    }
    double minDiff = abs(a-(t - ))
    // int minIndex = 0;
    // double minDiff = abs(a - (t - h[0] * 0.006));

    // for (int i = 1; i < n; i++) {
    //     double temp = t - h[i] * 0.006;
    //     double diff = abs(a - temp);
    //     if (diff < minDiff) {
    //         minDiff = diff;
    //         minIndex = i;
    //     }
    // }

    // cout << minIndex + 1 << endl;

    return 0;
}
